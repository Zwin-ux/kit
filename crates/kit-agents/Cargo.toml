[package]
name = "kitctl-agents"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Agent adapters for Codex, Claude Code, Grok Build, and Ollama"

[lib]
# Package renamed for crates.io (kit-cli is taken); code keeps `kit_agents`.
name = "kit_agents"

[dependencies]
kit-core.workspace = true
async-trait.workspace = true
thiserror.workspace = true
tokio.workspace = true
serde_json.workspace = true

# Process-tree kill for agent grandchildren (cmd → node → tools).
[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.59", features = [
    "Win32_Foundation",
    "Win32_Security",
    "Win32_System_JobObjects",
    "Win32_System_Threading",
] }

[target.'cfg(unix)'.dependencies]
libc = "0.2"
