# Session B — 2026-09-11 (blocked, not a receipt)

Tried live `target\debug\kit.exe run --agent grok|ollama --live --json` against a tiny git fixture with `kit.toml` `test = 'echo ok'`.

## What happened

1. `.\kit.cmd` hit **stale `target\release\kit.exe`** (M0 banner). Always use debug or rebuild release.
2. Current debug binary creates the worktree (`Preparing worktree` / `HEAD is now at … init`) then **prints nothing else**. No JSON envelope. No `~/.kit/runs/<new id>`. Latest receipt is still Session A `01M06A2PXBBH43ZFF3GJ9VQW94` (2026-08-16).
3. Grok hung ~7m after worktree (`grok -p --always-approve --output-format streaming-json`). Ollama hung ~5m the same way. `KIT_FULL_AUTO=1` did not change it.

## What this is not

Not Session B. CEO brief: dry-run / `--demo` / hung spawn does not count. `gateVacuous=false` never written.

## Follow-up (Power/Factory)

- Stream agent output to stderr during `--json` so a hang is visible.
- Confirm `grok --output-format streaming-json` is a real grok-build flag; drop it if it blocks.
- Session B retry: `cargo build -p kit-cli --release` then live run with a 2-minute kill.
