# QA / motion / high-quality check — 2026-08-16

Playbook rubric: `docs/dev/design-package/03-CURSOR-PLAYBOOK.md`.  
Session A receipt: `01M06A2PXBBH43ZFF3GJ9VQW94` (`gateVacuous: false`, PASS).

## 1. Automated QA (dirty tree)

| Check | Result |
|-------|--------|
| `cargo fmt --all --check` | pass |
| `cargo clippy -p kit-cli -p kit-tui --all-targets -- -D warnings` | pass |
| `cargo test -p kit-tui` | **75** passed (includes new motion-off test) |
| `cargo test -p kit-cli --bin kit -- demo_flag_is_tui` | pass |

**Stale-artifact trap:** `cargo test --workspace` first ran an old `kit-cli` (13 tests) and an old `kit-tui` (58 tests) from a shared `CARGO_TARGET_DIR`. Session A’s worktree `cargo test` had compiled HEAD into the same target dir. After `cargo clean -p kit-cli` and a rebuild from this repo, counts matched reality. Not a product gate miss — an agent-env inheritance issue. Recorded; engine not changed.

## 2. Motion / animation (M0)

| Claim | Evidence |
|-------|----------|
| One clock | Only `tokio::time::interval` in TUI is [`loop.rs`](../../crates/kit-tui/src/loop.rs). `event.rs` is untouched (Claude-only). |
| Tick is not redraw-worthy | `AppEvent::AnimationTick.is_redraw_worthy() == false` |
| Idle + motion on stays clean | `animation_tick_does_not_dirty_idle_room_even_with_motion` |
| Motion off + RUNNING stays clean | **added** `motion_off_running_row_does_not_dirty_on_tick` — pass |
| Live motion | Elapsed labels at 1 Hz while RUNNING/GATING; flash expiry. `Clock::blink` / `frame` unused in `ui/`. No mascot. |
| Engine timers | `kit-cli` uses `sleep`/`timeout` for run bounds and tests only — not a second TUI clock. |

**Idle CPU (empty room, debug `kit.exe`, 20s after 5s settle):** 0.063 processor-seconds → **0.31% of one core** (0.02% of 16 cores). Under the 1% kill.

**Demo CPU (after `--demo` fix, 10s):** **0.31% of one core.** Usable. No extra timers.

## 3. Human loop / first paint

| Path | Result |
|------|--------|
| `.\kit.cmd --version` | `kit 1.0.0-alpha.1` |
| Bare `kit --version` | `0.1.3` (npm) — identity defect, not fixed this cycle |
| `kit doctor` | Rust 1.0; warns npm `kit.cmd` + `kit` on PATH; all four agents **ready** |
| `doctor --json` | `schemaVersion: 1`, `pathCollisions` present |
| `kit --demo` (before fix) | **exit 2** — `unknown command: --demo` |
| `kit --demo` (after fix) | stays up; fixture Control Room |
| Snapshots 80×14 | FAIL selected; `^ tsc: 3 errors — Type '`; footer keeps `r` and `?`; help has kill/retry, no j/k |
| `NO_COLOR` | `Theme::monochrome` — modifiers only, no RGB wash (unit tests) |
| `KIT_MOTION=off` | reducer does not dirty on tick (idle or RUNNING) |

## 4. Fix this cycle (one slice)

Advertised `kit --demo` was parsed as a subcommand. [`main.rs`](../../crates/kit-cli/src/main.rs) now treats `--demo` / `-d` / `demo` as Control Room launch. Guard: `demo_flag_is_tui_not_unknown_command`.

No idle-CPU leak. No motion-off dirty. Second fix slot unused.

## 5. Playbook rubric

1. **Loop / honesty?** Yes — `--demo` is the 30-second product path; it actually opens the room. Motion/CPU claims are now measured.
2. **Engine still truth?** Yes. No contract edits. Persona still TUI-local.
3. **Reopened a settled decision?** No. No mascot, PTY, installer, vibe, or persona expansion.
4. **Incremental?** One parser arm + one reducer test.
5. **Evidence?** Tests above, doctor JSON, CPU samples, Session A receipt, snapshots.
6. **Biggest remaining gap?** Bare `kit` is still npm 0.1. GitHub About/topics still sell 0.1. Shared `CARGO_TARGET_DIR` can make workspace tests lie after a Kit-on-Kit gate. Live Session B not run (agents are ready — next when you want it).

## Verdict

**QA pass for this cycle.** M0 idle CPU holds. Reduced motion holds. First-paint `--demo` is no longer a lie. Do not tag alpha.2 until PATH/`kit` is the Control Room and a live Session B receipt exists.
