# Dogfood notes — 2026-08-13

Phase 1 of `docs/dev/design-package/03-CURSOR-PLAYBOOK.md`.

## Before

| Check | Result |
|-------|--------|
| Agents on PATH | codex 0.147.0, claude 2.1.220, grok 1.0.0, ollama 0.32.9 — all `ready` |
| `kit.toml` | Added this session: fmt + clippy -D warnings + `cargo test --workspace`, timeout 15m, firewall block |
| Workspace tests | See evidence below |

## Session A — offline proof

Prior to `kit.toml`, dry-run JSON was:

```json
"state": "pass",
"gateVacuous": true
```

That was inference being skipped on `--dry-run`. After `kit.toml`, Session A is re-run as a **real gate** (not vacuous). Receipt id recorded when that run finishes.

## Session B — live single agent

Not run yet. Needs a short task and will pay the 15m gate. Do not confuse with this notes file being written by the orchestrator.

## Session C — Control Room fan-out

Not run yet. Interactive TUI. Needs a human at the keyboard for `d` / `r` / `k`.

## Session D — answers so far

| Question | Answer |
|----------|--------|
| Agents used | doctor only (A pending re-run with kit.toml) |
| What worked | Honest review; kit.toml parses (`workspace_kit_toml_declares_a_real_gate`); engine tests isolated onto a bare git fixture so P3 does not run 12 workspace builds |
| What broke | Dry-run used to claim PASS while vacuous. Docs/wiki/todo were stale. README sold 0.1 below the fold |
| Gate caught something real? | Pending the post-kit.toml dry-run |
| Would you open Kit tomorrow? | Not yet — still a demo TUI until B/C happen |

## Kill criterion

One full session leaves useful receipts and at least one real gate lesson — **not met**.
