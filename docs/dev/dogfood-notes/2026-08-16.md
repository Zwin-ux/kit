# Dogfood — 2026-08-16

North star: widely used, high-quality developer tool. One `kit` verb. Prove the gate.

## PATH

| Invoke | Result |
|--------|--------|
| `kit` (this machine PATH) | npm `@mzwin/kit@0.1.3` — cut product |
| `.\kit.cmd --version` | `kit 1.0.0-alpha.1` (repo shim → Rust) |

GitHub About / topics / latest release still sell 0.1. Human-only.

## Session A (Kit-on-Kit dry-run)

**Status:** proven.

```text
cargo run -p kit-cli -- run --dry-run --task "dogfood 2026-08-16: prove kit.toml" --json
```

Worktree: detached `0567ca2`. Gate loaded from this repo's `kit.toml`. ~36s.

| Field | Value |
|-------|--------|
| receipt id | `01M06A2PXBBH43ZFF3GJ9VQW94` |
| dir | `~/.kit/runs/01M06A2PXBBH43ZFF3GJ9VQW94/` |
| state | `pass` |
| gateVacuous | `false` |
| gatePassed | `true` |
| checks | format 1s · typecheck 3s · test 31s — all pass |
| first failing check | none |

```text
kit receipt show 01M06A2PXBBH43ZFF3GJ9VQW94
```

## Still open

- Session B: one live `kit run --agent <ready>`
- Session C: human TUI (`.\kit.cmd --demo`) — not bare `kit` until PATH is Rust
- GitHub About / topics
- Commit + PR when asked
