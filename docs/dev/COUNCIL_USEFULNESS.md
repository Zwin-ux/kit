# Council decision — make Kit useful (2026-07-29)

Three seats reviewed the Action Terminal: **Product**, **Engineering**, **Design**.

## Consensus

| Rank | Expansion | Why |
|------|-----------|-----|
| 1 | **Job proof vault** | README says “keep the proof.” Logs died on quit. |
| 2 | **Ops write in-terminal** | Ready/Unify plan bounced to Home. Default surface must finish the job. |
| 3 | **Skill-as-job** (next) | Skills install but do not become the agent job yet. |

**Primary user:** multi-agent shipper who wires skills and runs one bounded job per intent.

**Kill this sprint:** more mascot polish, splash theater, social login as a gate, Workshop publish.

## Shipped from this council

1. **Proof vault** — `saveRun` / `listRuns` / `loadRun` under `~/.kit/runs/`.
2. **Agents · H History** — open a saved transcript into the LOG.
3. **Ops plan → y write** — Ready and Unify write without leaving the terminal.

## Next slice (not this pass)

**Skill-as-job:** pick a library skill + runner → inject skill into a fixed prompt envelope → inspect/build with confirm.

## STE copy rule

Short sentences. Active voice. One instruction per line. State, not hype.
