# Kit quality cycle — AUDIT ONLY

**Follow-up (Power, same day):** fixed footer ↑↓ select, help/SPEC j/k honesty, nested-q no quit, token-aware footer fit, help overlay snapshot, monochrome style tests. **P3 concurrency harness shipped** after this audit (`engine::supervisor` + 12-job proof). Re-audit tip before CEO merge.

## Truth
HEAD f2-f3-control-room-detail@8487d268 (PR #11 open vs main@d923e5b1). Alpha control room+engine+P1/P2/P4 code on branch only; version 1.0.0-alpha.1; +19k-line mega-PR not merged. Tip CI in_progress (unstable); last full Rust 3-OS green was prior SHA 4089e81. Live kill/8-way concurrency/distribution/Guardian-full not proven.

merge_readiness: NOT READY — wait tip 3-OS green, CEO kill-criteria/boundary review, then CEO-only merge. Incomplete live-kill and concurrency proofs remain stamp blockers if enforced strictly.

## Confirmed findings (11)
- **Control Room footer omits primary j/k nav** (surface-tui) — Read crates/kit-tui/src/ui/control_room.rs draw_footer hints " [d]ispatch  [b]oard  [enter] open  [g]ate  [k]ill  [r]etry  [?]help" (no ↑↓/j/k nav); snapshots kit_tui__ui__tests__populated_control_room_snapshot.snap / empty / narrow lock that string. docs/dev/SPEC-surface-1.0.md Control Room Keys table: "j`/`k` or arrows | Move selection"; Interaction grammar §3: Footer "[key] action" list, "never empty of discoverability". crates/kit-tui/src/ui/mod.rs help_lines ControlRoom: "  j/k ↑↓     move selection" only under ? overlay. crates/kit-tui/src/app.rs on_control_room_key: only KeyCode::Up/Down call move_selection; Char('k')/K is request_kill(); no Char('j') — help/SPEC overclaim j/k vs kill collision. Concept PRD/wiki footers also omit ↑↓ (match live); README.md and kit-cli print ↑↓ select outside TUI footer.
  - fix: 1) Footer: add an unambiguous selection hint first, e.g. " [↑↓] select  [d]ispatch  [b]oard  [enter] open  [g]ate  [k]ill  [r]etry  [?]help" (truncate on narrow width like other screens). 2) Do not advertise [k] as both nav and kill — drop j/k from SPEC + help unless kill is rebound (e.g. only arrows + optional j for down). 3) Align help_lines, SPEC keys table, and on_control_room_key; if j is kept, wire Char('j')→move_selection(1) and never Char('k') for up. 4) Update Control Room snapshots.
- **Footer mid-truncates at 60 cols** (surface-tui) — Verified read: crates/kit-tui/src/ui/common.rs draw_footer (L111-127) uses truncate(hints, width) only — no short-form/priority path; truncate (L16-27) is char-cap+…. control_room.rs draw_footer call (L61-67) always passes full " [d]ispatch  [b]oard  [enter] open  [g]ate  [k]ill  [r]etry  [?]help". Snapshot kit_tui__ui__tests__populated_control_room_snapshot_narrow.snap (60-col render from ui/mod.rs populated_control_room_snapshot_narrow) footer line ends "[r]etry…" and omits [?]help. Parallel: run_detail_stream_narrow_snapshot.snap ends "[k]ill  […". docs/dev/DESIGN-tui.md responsive table: 60–79 = "Narrow columns, more truncation"; density: footer one line, full help behind ?. docs/dev/SPEC-surface-1.0.md grammar: footer never empty of discoverability. No compressed-hint API/search hits in kit-tui.
  - fix: Add width-aware footer hints: e.g. footer_hints(width) with priority-ordered short forms (keep [?]help / [esc]/primary actions; drop lower-priority keys before mid-token truncate). Prefer token-aware fit over truncate(full_string, width). Update draw_footer callers (control_room, run_detail, board, dispatch) and refresh narrow insta snaps so 60-col footer still shows a complete compressed grammar, not "[r]etry…".
- **Concept density still scaffold-light** (surface-tui) — Verified in control_room.rs: Layout Constraint::Length(1) header + draw_header(..., "KIT / CONTROL ROOM", stats) (lines 24–47); row_for uses marker "▶ " + theme.selected_row() when selected, theme.fail_row(false) for Fail (124–136). common.rs draw_header is a single Paragraph line (title|stats), not multi-line brand/stats block. theme.rs selected_row = REVERSED|BOLD (118–120); fail_row applies .bg(self.fail_wash) when colored (177–185); Theme::kit fail_wash = Rgb(0x2a,0x12,0x16) (41). DESIGN-tui.md concept-control-room: "Dense ops table, FAIL row wash, cyan focus rail, stats header"; Selection: "Reverse video or accent left rail" — no rail path found in kit-tui. Snapshots empty/populated_control_room_snapshot.snap show one flat header line and plain FAIL row text only (no style/bg), so fail_wash cannot appear in insta text frames.
  - fix: Raise CR craft density toward DESIGN/concept without breaking PRD columns: (1) multi-line header (brand mark/title + stronger multi-stat strip) instead of Length(1) flat string; (2) optional cyan accent left rail on selected rows (accent fg/bg bar) while keeping ▶ or dropping it; keep REVERSED as monochrome fallback; (3) keep fail_row bg wash for FAIL rows and either style-aware snapshot asserts or a dual-path (text + style) test so wash regressions are visible; avoid inventing progress columns.
- **Help overlay has no snapshot proof** (surface-tui) — Verified: (1) crates/kit-tui/src/ui/mod.rs draw() if app.help_open { ... draw_help_overlay(... help_lines(app)) }; help_lines() returns screen-specific copy (Control Room / Run detail / Attached / Dispatch / Board) including "Kit Control Room — keys" and "Press Esc or ? to close". (2) crates/kit-tui/src/ui/common.rs draw_help_overlay renders Clear + Block title " help " + Paragraph. (3) crates/kit-tui/src/app.rs help_open field; on_key opens on '?' and dismisses Esc/?/q while open. (4) app.rs test question_mark_toggles_help_overlay only asserts help_open bools—never render_to_string. (5) ui/mod.rs tests module: 11 insta snapshots (empty/populated CR, run detail stream/gate/diff, attached, dispatch, board, narrow variants); none set help_open or send KeyCode::Char('?') before render_to_string; App::with_motion initializes help_open: false (app.rs). (6) ui/snapshots/ has 11 .snap files, none named help; only footer text "[?]help" appears in empty/populated CR snaps. (7) docs/dev/SPEC-surface-1.0.md: "? | Help overlay" and "Help overlay ?: ship with remake phase 6".
  - fix: Add insta UI snapshots that open help before render_to_string: e.g. app.update(Key '?') then assert_snapshot for Control Room (80xN and narrow), plus at least one other screen (RunDetail or Dispatch) so help_lines branches and overlay geometry (12/76/12 vertical, 8/84/8 horizontal) + title/copy are guarded. Keep question_mark_toggles_help_overlay for state; snapshots own craft/layout.
- **NO_COLOR monochrome path unproven at paint** (surface-tui) — theme.rs: Theme::resolve() returns Theme::monochrome() when NO_COLOR is set; monochrome() sets all Color fields incl. fail_wash to Reset and monochrome=true; state_style/gate_style/fail_row/annotation use BOLD/DIM when monochrome (selected_row always REVERSED|BOLD). theme tests only monochrome_has_no_rgb_wash (flag+fail_wash), kit_palette_has_accent, state_styles_differ_in_color_mode (Theme::kit only)—no mono style assertions, no resolve/NO_COLOR test. Paint: control_room.rs/run_detail.rs/ui/mod.rs call Theme::resolve() inside draw; ui/mod.rs snapshots (populated_control_room_snapshot, run_detail_gate_fail_snapshot, etc.) never set NO_COLOR/KIT_THEME=mono nor inject Theme::monochrome; TestBackend snaps are plain text (e.g. FAIL + ^ tsc annotation) without modifiers. crates/ grep: no paint test under monochrome/NO_COLOR.
  - fix: 1) Expand theme unit tests: under Theme::monochrome assert fail_wash=Reset; state_style(Fail)/gate_style("FAIL") include BOLD; annotation includes DIM; fail_row(false) BOLD; fail_row(true)/selected_row REVERSED|BOLD; optionally serial test Theme::resolve with NO_COLOR set. 2) Paint proof: refactor draw paths to accept &Theme (or theme on App) so tests can pass Theme::monochrome(), OR set NO_COLOR under a mutex and re-render; add populated CR snapshot + assert style matrix for selected FAIL row + annotation_row (or a focused unit on row_for/annotation_row with Theme::monochrome). Text-only insta alone is insufficient—assert Style/Modifier differences or capture style-aware output.
- **Wide run-detail stream|diff split missing** (surface-tui) — SPEC-surface-1.0.md L72: "Wide (≥100 cols): optional stream left + diff right." L139 open question: "Split stream|diff on wide detail: implement in remake phase 4 if free". DESIGN-tui.md L15 concept-run-detail "stream + diff"; L66 "≥ 100 | Optional run-detail split (stream | diff)". crates/kit-tui/src/ui/run_detail.rs draw_body (fn L178-254) matches DetailPane and renders one Block only; no Horizontal layout, no width>=100 branch. kit-tui grep: draw_body only in run_detail.rs; snapshots at 80/60 cols only (ui/mod.rs).
  - fix: In run_detail::draw_body (or draw), when body area.width >= 100 and pane is Stream or Diff (or always on wide), Layout::Horizontal split stream left + diff right with two bordered panes; keep Gate single-pane; add insta snapshot at ≥100 cols; leave Gate/narrow tabbed path unchanged.
- **C:\Users\mzwin\kit\crates\kit-tui\src\ui\control_room.rs — add j/k (or ↑↓) to CR footer hints so primary navigation is always discoverable without ?** (surface-tui) — Verified in code. CR footer at crates/kit-tui/src/ui/control_room.rs draw_footer is exactly: " [d]ispatch  [b]oard  [enter] open  [g]ate  [k]ill  [r]etry  [?]help" — no j/k and no ↑↓ for selection. Primary nav is implemented only as KeyCode::Up/Down in App::on_control_room_key (crates/kit-tui/src/app.rs ~511–518) calling move_selection; there is no Char('j') handler. Char('k')|'K' is request_kill() (~536), so vim j/k cannot be the advertised nav pair without colliding with kill. Help overlay still documents "j/k ↑↓ move selection" (crates/kit-tui/src/ui/mod.rs help_lines, ControlRoom arm ~64) while also listing "k kill selected run" (~69) — discoverability currently requires ?, and help overclaims j/k. docs/dev/SPEC-surface-1.0.md § footer: "[key] action list, dim; never empty of discoverability".
  - fix: In control_room.rs draw_footer, add explicit selection keys using ↑↓ (not j/k — k is kill): e.g. " [↑↓] select  [d]ispatch  [b]oard  [enter] open  [g]ate  [k]ill  [r]etry  [?]help". Optionally fix help_lines ControlRoom to "↑↓ move selection" (drop false j/k) unless j-only nav is added without rebinding k. Keep footer width-safe via existing draw_footer truncate in common.rs.
- **C:\Users\mzwin\kit\crates\kit-tui\src\ui\mod.rs — add help_open snapshot (and optionally Theme::monochrome populated CR) so craft + NO_COLOR are regression-proven** (surface-tui) — Read crates/kit-tui/src/ui/mod.rs: draw() paints help when app.help_open (Theme::resolve + draw_help_overlay + help_lines); #[cfg(test)] has empty/populated CR, run_detail_*, attached, dispatch, board snapshots only — no test sets help_open or snapshots overlay. ui/snapshots/ has 11 snaps, none *help*. app.rs question_mark_toggles_help_overlay only asserts help_open bool. theme.rs Theme::monochrome/resolve(NO_COLOR) exist; monochrome_has_no_rgb_wash is palette-only; no UI snapshot forces monochrome or Theme::monochrome on populated CR. populated_control_room_snapshot.snap is plain text cells under Theme::resolve default.
  - fix: In crates/kit-tui/src/ui/mod.rs tests: (1) open help on Control Room (KeyCode::Char('?') or app.help_open=true), render_to_string at fixed size, insta::assert_snapshot! for help overlay craft (title/keys). (2) Optional: paint populated CR under Theme::monochrome — requires inject/override so draw does not only use Theme::resolve() from env (or set NO_COLOR carefully in-process); accept TestBackend may not lock RGB but still guards mono layout/text.
- **Help advertises j/k move; k kills, j dead** (surface-ux) — crates/kit-tui/src/ui/mod.rs help_lines(ControlRoom) includes both Line::from("  j/k ↑↓     move selection") and Line::from("  k          kill selected run") — self-contradictory and false on j. crates/kit-tui/src/app.rs on_control_room_key: only KeyCode::Up/Down call move_selection(±1); KeyCode::Char('k') | KeyCode::Char('K') => self.request_kill(); no Char('j') arm (falls to _ => Action::None). crates/kit-tui/src/ui/control_room.rs footer is " [d]ispatch  [b]oard  [enter] open  [g]ate  [k]ill  [r]etry  [?]help" — lists kill, not j/k nav. request_kill kills selected active run (Running/Gating/Queued).
  - fix: In help_lines Control Room branch (ui/mod.rs), change move line to "  ↑↓         move selection" (drop j/k). Keep "  k          kill selected run" aligned with on_control_room_key and the footer. Do not add j/k nav unless kill is remapped elsewhere (footer/[k]ill already owns k). Add a unit/snapshot test that help overlay text for Control Room does not contain "j/k" for movement, or that k on Control Room still only produces kill semantics.
- **q quits from nested screens vs SPEC grammar** (surface-ux) — SPEC: docs/dev/SPEC-surface-1.0.md Interaction grammar §5: "q — quit only from Control Room (and disabled while attached)"; §4 Esc is back-one-level never quit from nested. IMPL app.rs: on_control_room_key (L507-509) correctly quits; on_detail_key (L775-777) sets should_quit=true + Action::Quit; on_board_key (L694-696) same; on_dispatch_key (L548-552) quits when dispatch.focus != DispatchFocus::Task; on_attached_key (L873-876) correctly refuses with set_flash("Esc to detach (q disabled while attached)") + Action::None. Tests: attach_and_detach_without_quit covers attached refuse only; no regression that nested screens must not quit.
  - fix: In kit-tui/src/app.rs remove Char('q')/('Q') → Quit from on_detail_key, on_board_key, and on_dispatch_key (leave Task-focus path typing q as text). Keep quit only in on_control_room_key; keep on_attached_key refuse+flash. Optionally flash "q quits from Control Room — Esc to go back" on nested q. Add unit tests: open Detail/Board/Dispatch (non-Task focus), key('q') => Action::None, !should_quit, screen unchanged. Align help/footer (ui/mod.rs) to "q quit (Control Room only; disabled while attached)".
- **Stop advertising j/k as selection move while k=kill: update help_lines (and SPEC) to arrows-only move + k kill, or rebind j (and a non-k move) without reclaiming kill.** (surface-ux) — REAL. crates/kit-tui/src/ui/mod.rs help_lines ControlRoom: Line "j/k ↑↓     move selection" and Line "k          kill selected run". docs/dev/SPEC-surface-1.0.md Keys table: "j`/`k` or arrows | Move selection" and "k | Kill selected active run". Actual on_control_room_key (crates/kit-tui/src/app.rs): only KeyCode::Up/Down call move_selection; KeyCode::Char('k')|'K' => request_kill(); no Char('j') anywhere in kit-tui. Footer control_room.rs already arrows-style + [k]ill without j/k selection.
  - fix: Align docs with runtime: in help_lines ControlRoom change "j/k ↑↓     move selection" to "↑↓         move selection" (keep "k kill"); in SPEC-surface-1.0.md Keys table drop j/k and list only arrows for Move selection. Do not rebind k off kill unless product reopens the key map. Add a snapshot or string assert on help overlay if present.

## Top blockers (truth)
- Tip CI must finish green on ubuntu+macos+windows (fmt/clippy/test/startup)
- CEO review of PR #11 boundaries + P1 kill criteria (Power cannot merge)
- P3 8-concurrent / dispatch-12 proof harness missing
- Live agent kill dogfood (real codex/claude) missing
- Demo deliberate FAIL gate path still open
- Post-merge 1.0.0: installers, security flags, README, Guardian suite, attach
