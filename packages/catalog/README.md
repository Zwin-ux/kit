# @kit-skills/catalog

Bundled starter packs and skill catalog for Kit.

Shipped as a dependency of `@kit-skills/cli` so `kit pack list` works after a global install.

Source of truth remains the monorepo `skills/` and `packs/` folders; this package is synced before pack/publish.
